==> the connection must be closed after responding to transfer-encoding // DONE
==> If bad request -> Connection: close and close the connection // DONE

==> Best Practices and Tips for multipart/form-data
- Always validate the uploaded file’s size, type, and content.
- Use a random or unique naming strategy for uploaded files to avoid overwriting existing files.
- For security, avoid directly exposing uploaded files and always sanitize file names.

<!-- Some notices in config parsing -->
- Expand the x to the correct number

#include "../../includes/webserv.hpp"

class ConfigManager
{
private:
    /* data */
public:
    ConfigManager(/* args */);
    ~ConfigManager();
};

ConfigManager::ConfigManager(/* args */)
{
}

ConfigManager::~ConfigManager()
{
}
